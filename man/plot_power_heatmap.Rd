% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_power_heatmap}
\alias{plot_power_heatmap}
\title{Plot a power heatmap across two parameters}
\usage{
plot_power_heatmap(
  grid_result,
  x = "n_post",
  y = "level_change",
  xlab = x,
  ylab = y,
  main = "ITS power heatmap",
  palette = NULL
)
}
\arguments{
\item{grid_result}{A data frame as returned by \code{\link[=run_power_grid]{run_power_grid()}}, with a
\code{power} column and at least the columns specified by \code{x} and
\code{y}.}

\item{x}{Character. Name of the column to use as the x-axis variable.}

\item{y}{Character. Name of the column to use as the y-axis variable.}

\item{xlab}{Character. x-axis label. Defaults to \code{x}.}

\item{ylab}{Character. y-axis label. Defaults to \code{y}.}

\item{main}{Character. Plot title. Default \code{"ITS power heatmap"}.}

\item{palette}{Character vector of colours for the gradient from 0 to 100\\%
power. Defaults to a white-steelblue ramp.}
}
\value{
Invisibly \code{NULL}. Called for its side-effect (plot).
}
\description{
Displays estimated power as a colour-coded grid, with one parameter on each
axis. Useful for visualising how power responds to combinations of effect
size, follow-up duration, noise, or autocorrelation.
}
\examples{
\dontrun{
grid <- build_param_grid(
  n_post       = c(12, 18, 24, 30, 36),
  level_change = c(-1, -2, -3),
  sigma        = 2.5,
  rho          = 0.4
)
results <- run_power_grid(grid, n_sim = 300)
plot_power_heatmap(results, x = "n_post", y = "level_change")
}

}
\seealso{
\code{\link[=run_power_grid]{run_power_grid()}}, \code{\link[=build_param_grid]{build_param_grid()}}
}
