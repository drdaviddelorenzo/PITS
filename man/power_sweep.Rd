% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_power.R
\name{power_sweep}
\alias{power_sweep}
\title{Design optimisation sweep: power across a range of n_post values}
\usage{
power_sweep(sweep_post = c(6L, 12L, 18L, 24L, 30L, 36L), ..., verbose = TRUE)
}
\arguments{
\item{sweep_post}{Integer vector. \code{n_post} values to evaluate.
Default \code{c(6, 12, 18, 24, 30, 36)}.}

\item{...}{All other arguments passed to \code{\link[=calculate_power]{calculate_power()}}. Note that
\code{n_post} is overridden by \code{sweep_post} internally - do not
pass it separately.}

\item{verbose}{Logical. If \code{TRUE} (default), prints a formatted
sweep table to the console.}
}
\value{
A data frame with columns:
\describe{
\item{n_post}{Integer. Follow-up duration evaluated.}
\item{power}{Numeric. Estimated power (0-1).}
\item{power_pct}{Numeric. Power as a percentage.}
\item{interpretation}{Character. Qualitative label.}
\item{n_converged}{Integer. Number of converged replications.}
}
}
\description{
Runs \code{\link[=calculate_power]{calculate_power()}} for a vector of post-intervention durations,
returning a data frame of power estimates. Use this to identify the minimum
\code{n_post} needed to achieve \eqn{\ge 80\%} power.
}
\examples{
# Small n_sim for a fast example; use 1000+ for a reportable estimate.
sweep <- power_sweep(
  sweep_post   = c(12, 24, 36),
  n_pre        = 24,
  baseline     = 15,
  level_change = -3,
  sigma        = 2.5,
  rho          = 0.4,
  n_sim        = 100,
  seed         = 42,
  verbose      = FALSE
)
plot_power_curve(sweep)

}
\seealso{
\code{\link[=calculate_power]{calculate_power()}}, \code{\link[=plot_power_curve]{plot_power_curve()}}
}
