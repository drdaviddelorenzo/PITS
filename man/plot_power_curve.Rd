% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_power_curve}
\alias{plot_power_curve}
\title{Plot power as a function of post-intervention duration}
\usage{
plot_power_curve(
  sweep_result,
  target = 80,
  xlab = "Post-intervention time points (n_post)",
  ylab = "Estimated power (\%)",
  main = "ITS power curve",
  col = "steelblue",
  ...
)
}
\arguments{
\item{sweep_result}{A data frame as returned by \code{\link[=power_sweep]{power_sweep()}}, with
columns \code{n_post} and \code{power_pct}.}

\item{target}{Numeric. Power target line (0-100). Default 80.}

\item{xlab}{Character. x-axis label. Default
\code{"Post-intervention time points (n_post)"}.}

\item{ylab}{Character. y-axis label. Default \code{"Estimated power (\%)"}.}

\item{main}{Character. Plot title. Default \code{"ITS power curve"}.}

\item{col}{Character. Line colour. Default \code{"steelblue"}.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
Invisibly \code{NULL}. Called for its side-effect (plot).
}
\description{
Produces a line plot of estimated power against \code{n_post}, as returned
by \code{\link[=power_sweep]{power_sweep()}}. Highlights the 80\\% threshold and indicates the minimum
adequate duration.
}
\examples{
sweep <- power_sweep(
  sweep_post = c(12, 18, 24, 30, 36),
  n_pre = 24, baseline = 15, level_change = -3,
  sigma = 2.5, rho = 0.4, n_sim = 200, seed = 42
)
plot_power_curve(sweep)

}
\seealso{
\code{\link[=power_sweep]{power_sweep()}}, \code{\link[=plot_power_heatmap]{plot_power_heatmap()}}
}
