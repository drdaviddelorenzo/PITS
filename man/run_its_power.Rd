% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{run_its_power}
\alias{run_its_power}
\title{Full single-site ITS power workflow}
\usage{
run_its_power(
  n_pre,
  n_post,
  baseline,
  level_change,
  slope_change = 0,
  sigma,
  rho,
  test = c("level", "slope", "both"),
  alpha = 0.05,
  n_sim = 1000L,
  seed = 123L,
  sweep = FALSE,
  sweep_post = c(6L, 12L, 18L, 24L, 30L, 36L),
  save_output = FALSE,
  output_dir = "pits_output"
)
}
\arguments{
\item{n_pre}{Integer. Pre-intervention time points.}

\item{n_post}{Integer. Post-intervention time points (primary design).}

\item{baseline}{Numeric. Baseline outcome.}

\item{level_change}{Numeric. Expected level change (your clinical hypothesis).}

\item{slope_change}{Numeric. Expected slope change. Default 0.}

\item{sigma}{Numeric. Residual SD.}

\item{rho}{Numeric. AR(1) autocorrelation.}

\item{test}{Character. Effect to test. Default \code{"level"}.}

\item{alpha}{Numeric. Significance threshold. Default 0.05.}

\item{n_sim}{Integer. Monte Carlo replications. Default 1000.}

\item{seed}{Integer. Random seed. Default 123.}

\item{sweep}{Logical. If \code{TRUE}, also run \code{\link[=power_sweep]{power_sweep()}}.}

\item{sweep_post}{Integer vector. \code{n_post} values for the sweep.}

\item{save_output}{Logical. If \code{TRUE}, save results via
\code{\link[=export_results]{export_results()}}.}

\item{output_dir}{Character. Directory for saved files.}
}
\value{
Invisibly, a list with elements \code{result} (from
\code{\link[=calculate_power]{calculate_power()}}) and, if \code{sweep = TRUE}, \code{sweep}
(from \code{\link[=power_sweep]{power_sweep()}}).
}
\description{
Convenience wrapper that replicates the interactive experience of
\code{its_power_tool.R}. Runs the power simulation and optionally a design
sweep, prints formatted output to the console, and optionally saves results.
}
\examples{
\donttest{
run_its_power(
  n_pre = 24, n_post = 24,
  baseline = 15, level_change = -3,
  sigma = 2.5, rho = 0.4,
  n_sim = 100, sweep = TRUE
)
}

}
\seealso{
\code{\link[=calculate_power]{calculate_power()}}, \code{\link[=power_sweep]{power_sweep()}}, \code{\link[=estimate_its_params]{estimate_its_params()}}
}
