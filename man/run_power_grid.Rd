% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_power.R
\name{run_power_grid}
\alias{run_power_grid}
\title{Run power calculations across a parameter grid}
\usage{
run_power_grid(
  grid,
  test = c("level", "slope", "both"),
  alpha = 0.05,
  n_sim = 500L,
  seed = 123L,
  verbose = TRUE
)
}
\arguments{
\item{grid}{A data frame as returned by \code{\link[=build_param_grid]{build_param_grid()}}, or any data
frame with columns \code{n_pre}, \code{n_post}, \code{baseline},
\code{level_change}, \code{slope_change}, \code{sigma}, \code{rho}.}

\item{test}{Character. Effect to test: \code{"level"}, \code{"slope"}, or
\code{"both"}.}

\item{alpha}{Numeric. Significance threshold. Default 0.05.}

\item{n_sim}{Integer. Monte Carlo replications per cell. Default 500.}

\item{seed}{Integer. Base random seed; each row adds its row index.}

\item{verbose}{Logical. If \code{TRUE}, prints a progress counter.}
}
\value{
The input \code{grid} data frame with columns appended:
\code{power}, \code{power_pct}, \code{interpretation}, \code{n_converged}.
}
\description{
Applies \code{\link[=calculate_power]{calculate_power()}} to each row of a parameter grid as produced by
\code{\link[=build_param_grid]{build_param_grid()}}, returning a data frame with the estimated power for
each combination.
}
\examples{
\dontrun{
grid <- build_param_grid(
  n_post       = c(12, 24, 36),
  level_change = c(-2, -3),
  sigma        = 2.5,
  rho          = 0.4
)
results <- run_power_grid(grid, n_sim = 500)
plot_power_heatmap(results, x = "n_post", y = "level_change")
}

}
\seealso{
\code{\link[=build_param_grid]{build_param_grid()}}, \code{\link[=plot_power_heatmap]{plot_power_heatmap()}}
}
