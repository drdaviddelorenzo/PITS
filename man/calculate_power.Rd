% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_power.R
\name{calculate_power}
\alias{calculate_power}
\title{Estimate statistical power for a single-site ITS design}
\usage{
calculate_power(
  n_pre,
  n_post,
  baseline,
  level_change,
  slope_change = 0,
  sigma,
  rho,
  test = c("level", "slope", "both"),
  alpha = 0.05,
  n_sim = 1000L,
  seed = 123L,
  pre_trend = 0
)
}
\arguments{
\item{n_pre}{Integer. Number of pre-intervention time points.}

\item{n_post}{Integer. Number of post-intervention time points. This is
the primary design lever: use \code{\link[=power_sweep]{power_sweep()}} to find the minimum
\code{n_post} that achieves \eqn{\ge 80\%} power.}

\item{baseline}{Numeric. Mean outcome in the pre-intervention period.}

\item{level_change}{Numeric. Expected immediate step change at the
intervention point (your minimum clinically meaningful effect). Set to
0 when \code{test = "slope"}.}

\item{slope_change}{Numeric. Expected change in trend per time unit after
the intervention. Default 0. Set to 0 when \code{test = "level"}.}

\item{sigma}{Numeric. Residual standard deviation. Estimate from
pre-intervention data using \code{\link[=estimate_sigma]{estimate_sigma()}} or \code{\link[=estimate_its_params]{estimate_its_params()}}.}

\item{rho}{Numeric. AR(1) autocorrelation coefficient in \eqn{[0, 1)}.
Use 0.4 as a conservative default for monthly data if unknown.}

\item{test}{Character. Effect to test: \code{"level"} (default),
\code{"slope"}, or \code{"both"}. See \code{\link[=fit_its_model]{fit_its_model()}} for details.}

\item{alpha}{Numeric. Significance threshold. Default 0.05.}

\item{n_sim}{Integer. Number of Monte Carlo replications. Use 500 for a
quick check, 1000 for a reportable estimate, 2000+ for publication.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{pre_trend}{Numeric. Pre-intervention trend per time unit. Default 0.}
}
\value{
A named list:
\describe{
\item{power}{Numeric. Estimated power (proportion of simulations
with \eqn{p < \alpha}).}
\item{power_pct}{Numeric. Power as a percentage.}
\item{interpretation}{Character. Qualitative label (see
\code{\link[=interpret_power]{interpret_power()}}).}
\item{p_values}{Numeric vector. Raw p-values from all \code{n_sim}
replications (\code{NA} = non-convergence).}
\item{n_converged}{Integer. Number of replications that converged.}
\item{n_sim}{Integer. Total replications requested.}
\item{params}{Named list. Input parameters, for traceability.}
}
}
\description{
Runs a Monte Carlo simulation to estimate the probability of detecting a
specified intervention effect in a single-site ITS study.
}
\examples{
result <- calculate_power(
  n_pre = 24, n_post = 24,
  baseline = 15, level_change = -3,
  sigma = 2.5, rho = 0.4,
  n_sim = 500, seed = 42
)
print(result$power_pct)

}
\seealso{
\code{\link[=power_sweep]{power_sweep()}}, \code{\link[=calculate_power_multi]{calculate_power_multi()}}, \code{\link[=estimate_its_params]{estimate_its_params()}}
}
