% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{estimate_and_calculate}
\alias{estimate_and_calculate}
\title{Estimate parameters and calculate power in one step}
\usage{
estimate_and_calculate(
  data,
  level_change,
  n_post,
  outcome_col = "outcome",
  time_col = "time",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{Pre-intervention data: a data frame with columns \code{time}
and \code{outcome}, or a numeric vector of outcome values.}

\item{level_change}{Numeric. Minimum clinically meaningful effect size
(your clinical hypothesis). This is \strong{not} estimated from data.}

\item{n_post}{Integer. Planned post-intervention follow-up duration.}

\item{outcome_col}{Character. Name of the outcome column. Default
\code{"outcome"}.}

\item{time_col}{Character. Name of the time column. Default \code{"time"}.}

\item{verbose}{Logical. Print parameter and power summaries. Default
\code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=calculate_power]{calculate_power()}}.}
}
\value{
Output from \code{\link[=calculate_power]{calculate_power()}}.
}
\description{
Convenience function that chains \code{\link[=estimate_its_params]{estimate_its_params()}} and
\code{\link[=calculate_power]{calculate_power()}}. Supply a pre-intervention dataset, a
\code{level_change}, and a target \code{n_post}, and receive a power
estimate.
}
\examples{
data("example_cfr_data")
result <- estimate_and_calculate(
  data         = example_cfr_data,
  level_change = -1.0,
  n_post       = 24
)
result$power_pct

}
\seealso{
\code{\link[=estimate_its_params]{estimate_its_params()}}, \code{\link[=calculate_power]{calculate_power()}}, \code{\link[=run_its_power]{run_its_power()}}
}
