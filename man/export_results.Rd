% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{export_results}
\alias{export_results}
\title{Export PITS results to CSV and plain-text summary}
\usage{
export_results(result, dir = "pits_output", prefix = "pits")
}
\arguments{
\item{result}{Output from \code{\link[=calculate_power]{calculate_power()}} or \code{\link[=power_sweep]{power_sweep()}}.}

\item{dir}{Character. Output directory. Created if it does not exist.
Default \code{"pits_output"}.}

\item{prefix}{Character. File name prefix. Default \code{"pits"}.}
}
\value{
Invisibly, a named character vector of file paths written.
}
\description{
Writes the output of \code{\link[=calculate_power]{calculate_power()}} or \code{\link[=power_sweep]{power_sweep()}} to timestamped
files in the specified directory.
}
\examples{
\donttest{
result <- calculate_power(n_pre = 24, n_post = 24, baseline = 15,
                          level_change = -3, sigma = 2.5, rho = 0.4,
                          n_sim = 100)
# Write to a temporary directory (use your own path in practice):
export_results(result, dir = tempdir())
}

}
